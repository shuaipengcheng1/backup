<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.pulllist.Dao.PullDao">
    <select id="pull" resultType="com.example.pulllist.Domain.PullItem">
        select *
        from pullblank
        where goods_type = #{arg0}
  </select>
</mapper>